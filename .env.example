# Copy to .env and fill in. .env is gitignored and is NEVER read into a chat transcript.
#
#   cp .env.example .env && ${EDITOR:-nano} .env
#
# The benchmark reads this file directly; the key stays on your machine.

GEMINI_API_KEY=
